const app = document.querySelector<HTMLElement>('#app')

if (app) {
  app.innerHTML = `
    <h1>Censo Vivo Ecuador</h1>
    <p>Fuentes y cartografía en verificación. Todavía no hay datos publicados.</p>
  `
}
